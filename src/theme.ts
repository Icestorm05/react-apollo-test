import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#0c499c"
        },
        secondary: {
            main: "#0080c9"
        },
        error: {
            main: "#e71f85"
        }
    }
});

export default theme;
